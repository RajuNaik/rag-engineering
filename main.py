"""Future RAG application entry point.

For now, run source-specific ingestion examples directly. The application
entry point will be connected after retrieval and generation are implemented.
"""


if __name__ == "__main__":
    print("RAG Engineering project is currently in the ingestion stage.")
    print("Run the individual ingestion modules while learning each source.")
